# HireLens dependencies
# Install with:  pip install -r requirements.txt
# Tested on Python 3.10 - 3.12.

# --- Web application ---
streamlit>=1.40.0        # 1.34-1.39 let `except Exception` swallow st.rerun()
plotly>=5.18.0
pillow>=10.0.0

# --- Data & document processing ---
pandas>=2.0.0
pdfplumber>=0.10.0
pypdf>=4.0.0            # successor to PyPDF2, which is unmaintained
openpyxl>=3.1.0         # required by pandas .to_excel() / .read_excel()
matplotlib>=3.7.0       # required by DataFrame.style.background_gradient()
pyyaml>=6.0

# --- Multi-agent orchestration & AI ---
crewai[azure-ai-inference]>=0.28.0   # extra is required: crewai routes "azure/*"
                                     # models to a native provider that raises
                                     # ImportError without it
openai>=1.10.0          # provides the AzureOpenAI client
pydantic>=2.0
python-dotenv>=1.0.0
